name: 'Build Maven project'
description: 'Build Maven project skipping tests'

runs:
  using: 'composite'
  steps:
    - name: Build with Maven
      run: ./mvnw -B clean package -DskipTests -DskipITs
      shell: bash
